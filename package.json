{
  "name": "tailcat-local-wasm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node wasm_client/build.mjs",
    "test": "node --test wasm_client/test/*.test.mjs",
    "check": "node wasm_client/verify.mjs"
  },
  "engines": {
    "node": ">=22.15.0"
  },
  "dependencies": {
    "ssh2": "1.17.0",
    "undici": "7.29.0"
  }
}
